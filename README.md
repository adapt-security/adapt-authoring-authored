# adapt-authoring-authored

Adds authoring metadata to other modules' data. Augments existing schemas with attributes describing *how and when* a document was authored — such as its creator and created/updated timestamps.

Extends `AbstractModule` from [adapt-authoring-core](../adapt-authoring-core); other modules opt in so their documents carry consistent authoring information.
